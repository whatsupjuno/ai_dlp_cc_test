<?xml version="1.0" encoding="UTF-8"?>
<!--
  Entitlements for the containing menu-bar app (SentinelAgent).
  The app installs/activates the system extension and shares an App Group with
  it for policy/audit exchange. Requires a Developer ID with the System
  Extension and Network Extension capabilities.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.developer.system-extension.install</key>
    <true/>
    <!-- Required so the app can configure the content filter via NEFilterManager
         (saveToPreferences). Without this the in-app activation path fails unless
         the MDM WebContentFilter payload is installed separately. -->
    <key>com.apple.developer.networking.networkextension</key>
    <array>
        <string>content-filter-provider-systemextension</string>
    </array>
    <key>com.apple.security.application-groups</key>
    <array>
        <string>group.com.sentinel.dlp</string>
    </array>
    <key>com.apple.security.app-sandbox</key>
    <false/>
</dict>
</plist>
